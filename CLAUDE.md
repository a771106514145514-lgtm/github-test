# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository state

This repository currently contains a single static file, `index.html`, used to verify that the GitHub connection/integration works. There is no build system, package manager, dependency manifest, test suite, or linter configured — there is nothing to build, install, lint, or test.

Because the repo has no established structure or conventions yet, don't assume patterns from other projects apply here. If the user asks you to scaffold a real application into this repo, ask what stack/framework they want before introducing tooling, since none currently exists.
